#include <stdio.h>
#include <string.h>

// a = 'Casa'
// b = 'Lucas'
// b = a + b
// a = b[len(a):len(b)]
// b = b[0:len(b)-len(a)]
// print(a,b)

int main() {
    char a[] = "Casa";
    char b[] = "Lucas";
    memccpy
    printf("%s %s", a, b);
    return 0;
}

